---
import { mockSocials } from "@data/socials.mock";
import ButtonLink from "./ButtonLink.astro";
---

<div class="flex items-center gap-2">
  {
    mockSocials?.map(({ href, icon: Icon }) => (
      <ButtonLink href={href}>
        <Icon className="size-5" />
      </ButtonLink>
    ))
  }
</div>
