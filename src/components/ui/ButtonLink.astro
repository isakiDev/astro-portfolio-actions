---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"a"> {}

const props = Astro.props;

const default_class =
  "inline-flex items-center text-sm justify-center rounded-xl bg-neutral-800 p-2 text-neutral-50 opacity-85 transition-opacity duration-150 hover:opacity-100 hover:cursor-pointer font-semibold";
---

<a {...props} target="_blank" class={default_class}>
  <slot />
</a>
